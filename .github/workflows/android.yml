name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Spotless check
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6.0.2
      - name: Set up JDK
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: 17
      - uses: gradle/gradle-build-action@v3.5.0
      - name: spotless
        run: ./gradlew spotlessCheck

  api_check:
    name: API check
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6.0.2
      - name: Set up JDK
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: 17
      - uses: gradle/gradle-build-action@v3.5.0
      - name: API check
        run: ./gradlew apiCheck

  unit_tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6.0.2
      - name: Set up JDK
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: 17
      - uses: gradle/gradle-build-action@v3.5.0
      # `desktopTest` runs the whole suite through Skiko, including the shape geometry and
      # the Compose UI tests. `testDebugUnitTest` runs the platform independent half again
      # against the Android artifact.
      - name: Run unit tests
        run: ./gradlew desktopTest testDebugUnitTest --stacktrace

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - name: set up JDK
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: 17

      - uses: gradle/gradle-build-action@v3.5.0
      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: |
          ./gradlew --scan --stacktrace \
              assemble